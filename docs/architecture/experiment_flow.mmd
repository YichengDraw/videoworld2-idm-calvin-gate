flowchart TB
  start["Sync latest GitHub main"] --> audit["Audit code, configs, artifacts"]
  audit --> guards["Add reliability guards"]
  guards --> tests["Run unit and compile validation"]
  tests --> rerun{"Raw CALVIN data/env available within 50 CNY?"}
  rerun -->|yes| real["Run fixed real CALVIN closed-loop"]
  rerun -->|no| offline["Keep offline CALVIN and mock-smoke boundary"]
  real --> table["Regenerate result tables"]
  offline --> table
  table --> report["Refresh report and README"]
  report --> cleanup["Remove stale public-repo noise"]
  cleanup --> push["Commit and push verified repo"]
