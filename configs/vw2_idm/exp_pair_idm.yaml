extends:
  - data_mock_smoke.yaml
  - idm_small.yaml

experiment_name: pair_idm

idm:
  variant: pair
  use_future_codes: true
  use_past_actions: false
  code_source: gt
  mixed_code_training: false

training:
  output_dir: ../../outputs/vw2_idm/pair_idm
